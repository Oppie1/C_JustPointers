#include<stdio.h>
#include<stdlib.h>



int main() {

	//Declare uninitialized int var for loop counter as well as int
	//var to store how many numbers the user wants to average.
	//CODE:

	//Declare an int var named total and initialize it to 0.
	//CODE:

	//Declare a float var named avg an initialize it to 0.
	//CODE:

	//Declare uninitialized int pointer named pointsArray.
	//CODE:

	printf("\nHow many numbers do you want to average?\n\n");

	//Call the scanf_s() to bring in info to memory address of how many.
	//CODE:

	//Assign pointsArray int pointer to expression that will request memory from 
	//heap at runtime -> remember int* type cast malloc(2parameters) 
	//CODE:


	printf("\nGo ahead and enter those numbers.\n\n");

	//Construct for loop that loops through user entries with counter 
	//starting at 0, comparing counter to howMany and increment it by 1.
	//CODE:

		//Scan in the user chosen numbers into the memory location of the given element 
		//of the pointsArray.
		//CODE:

		//Assign total to expression taking last value entered in points array elements and adds it to the current total.
		//CODE:
	//call printf() to display the final total to the screen.
	//CODE:

	//Assign var average to the result of the total divided by how may numbers
	//the user entered. float typecast.
	//CODE:

	//Call printf() to display the average to the screen.
	//CODE:

}