#include<stdio.h>
#include<stdlib.h>
//Include the header file for employee info so the struct definition is available in this file. ""
//CODE:
#include "EmployeeInfo.h"


int main() {

	//Declare two variables of type struct user, each one is independent instance 
	//of the blueprint -> copilot and claude.
	//CODE:

	//ASSIGN copilot and claude user id, age and weight to decimal.
	//CODE:

	//Use a puts() to prompt user for name of user 1
	//CODE:
	puts("What is the user 1's name\n");

	//Use a gets() to read in information from struct copilot -> fistName (.)
	//CODE:

	puts("Enter the name of user 2\n");

	//Use gets() to read in information from struct claude -> firstName.
	//CODE:

	//Use two printf() to display the information to the screen using the copilot and claude. (.)
	//and then both user id, age, weight and Name members
	//CODE:

}