#include<stdio.h>


int main() {

	//Declare a char array named movie capable of holding 20 characters.
	//CODE:

	//Declare a char pointer named pMovie initialize it to the movie array. & is not needed because
	//the name of an array is already its memory address.
	//CODE:

	//Call a puts() function to prompt user to enter a movie title.
	//CODE:

	//Call an fgets() function with arguments pMovie as the storage var that can hold 20 characters from standard input keyboard (stdin)
	//CODE:

	//Call a puts() with the pointer movie argument to show info you just brought in to the screen.
	//CODE:

}