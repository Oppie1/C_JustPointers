#include<stdio.h>




int main()
{
	//Declare int var initialized to 19.
	//CODE:

	//Declare pointer var pTuna. Initialize it to memory address of tuna.
	//CODE:

	printf("Address \t\t Name \t\tValue \n");

	//Call a printf() display  pTuna, "tuna", tuna. Use pointer, string and int.
	//CODE:

	//Call printf() to display difference in location of memory address in first argument
	//vs the pTuna variable itself in the third argument %p
	//CODE:

	//Use the * pointer and pTuna to overwrite 19 by reinitializing it to 71. Notice how data type in is 
	//known as usual and does not need to be declared again.
	//CODE:

	printf("\nThe new value of *pTuna AND tuna is now:\n");

	//Call print() to %d with matching pTuna (*) pointer var to display the value of pTuna AND tuna is to screen.
	//tuna updates its value as well.
	//CODE:

}