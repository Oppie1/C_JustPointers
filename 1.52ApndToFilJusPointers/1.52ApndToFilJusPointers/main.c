#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<stdlib.h>



int main() {

	//Declare uninitialized FILE pointer. Name it fPointer
	//CODE:

	//Assign fPointer to fopen() with "AppendToAFile.txt" in first arg and "a" in second arg.
	//CODE:

	//Use if condition to test if fPointer exists == NULL and is open.
	//CODE:

	//Call printf() to display message when if condition is true and executes (error: file could not not opened)
	//CODE:

	//returning 1 to signal program failed.
	//CODE:

	//Call fprintf() with pointer and sentence that will be printed to file.
	//CODE:

	//Call the fclose() with the pointer as the argument.
	//CODE:

	printf("\nTest successfully appended the file.");

}